`p:\AutoLISP_工装绘图项目\XBA_下摆凹.lsp#L863-866` 

要求：修改 `p:\AutoLISP_工装绘图项目\XBT_下摆凸.lsp` - 支持参数传递 ：
- 将主绘图函数修改为 (defun xbt (r0 a0 t0 b0 tool_type tech_choice custom_tech_text / ...) 。
- 在函数内部增加了参数检查逻辑：如果传入参数为 nil （例如从 CAD 命令行直接启动），程序会自动提示用户输入；如果参数已由 Python 传递，则直接跳过输入环节。
- 增加标准入口函数 ：
- 添加了 (defun c:xbt () ...) 作为 CAD 命令行入口。
- 该入口函数通过调用 (xbt nil nil nil nil nil nil nil) 来实现交互式绘图，保持了向后兼容性
增加命令行包装函数，
 `p:\AutoLISP_工装绘图项目\XBA_下摆凹.lsp#L863-866`