1.优化一下，主要包含下面几个方面：简化冗余代码，取消不必要的错误检查，提高和autocad的连接速度。
2.将GUI中的“初始化（新建图形）”拆解成两个部分：“新建图形”和“加载lisp文件”其中应用样板的功能在“新建图形”中实现。
3.由python文件
调用，要求：所有的UI组件都允许输入负值，中的参数radius为用户在GUI中输入的r0，chord_length为用户输入的a0，darwing_type为lisp文件中的工装类型参数drawing_prefix ，例如：“GPMJX”'QP“。

但是考虑到lisp文件中都是使用的正值参数，因此在从UI用户输入传入参数到lisp文件中调用相关函数时，需要先将参数转化为绝对值。


4.设计一个功能，输入镜片的半径，厚度和口径后，根据这些参数自动判断选用何种工装，并根据公式生成推荐的工装参数。
5.根据推荐的工装类型和参数选择lisp绘图程序并按顺序执行。